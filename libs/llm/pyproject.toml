[project]
name = "llm"
version = "0.1.0"
description = "Shared LLM abstractions and implementations"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.69.0",
    "pydantic-ai>=1.0.15",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "pytest-asyncio>=1.2.0",
]
