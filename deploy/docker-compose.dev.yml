services:
  telegram_bot:
    build:
      context: ..
      dockerfile: apps/telegram_bot/Dockerfile
    container_name: alfred-telegram-bot
    restart: unless-stopped
    env_file:
      - ../.env
    volumes:
      - ../logs:/app/logs
